apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "OpenTwins.fullname" . }}-test-connection"
  labels:
    {{- include "OpenTwins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "OpenTwins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
